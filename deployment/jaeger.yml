apiVersion: jaegertracing.io/v1
kind: Jaeger
metadata:
  name: jaeger
spec:
  strategy: production
  collector:
    replicas: 1
    image: jaegertracing/jaeger-collector
    options:
      collector:
        num-workers: 200
        queue-size: 11000
  storage:
    type: elasticsearch # <1>
    options:
      es:
        server-urls: https://quickstart-es-http.observability.svc.cluster.local:9200
        index-prefix: otel-tracing
        version: 7
        tls:
          skip-host-verify: true
    secretName: jaeger-secret
  volumeMounts:
    - name: certificates
      mountPath: /es/certificates/
      readOnly: true
  volumes:
    - name: certificates
      secret:
        secretName: quickstart-es-http-certs-public
  ingress:
    enabled: true

  # pcrb4xWmw7z10G106hRuK144
  # kubectl create secret generic jaeger-secret --from-literal=ES_PASSWORD=c6kKnoYrfEs107SV4X17116N --from-literal=ES_USERNAME=elastic -n observability
